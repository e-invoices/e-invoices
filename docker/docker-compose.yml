name: e-invoices

services:
  backend:
    build:
      context: ..
      dockerfile: docker/backend/Dockerfile
      target: ${APP_ENV:-development}
    container_name: backend-invoice
    image: backend-invoice
    env_file:
      - ../backend/.env
    environment:
      - APP_ENV=${APP_ENV:-development}
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ../backend:/app

  frontend:
    build:
      context: ..
      dockerfile: docker/frontend/Dockerfile
      target: ${APP_ENV:-development}
    container_name: frontend-invoice
    image: frontend-invoice
    ports:
      - "5173:5173"
    volumes:
      - ../frontend:/app
      - /app/node_modules
    depends_on:
      - backend
    environment:
      - VITE_API_URL=http://localhost:8000

  db:
    image: postgres:latest
    container_name: database-invoice
    restart: unless-stopped
    env_file:
      - ../backend/.env
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
